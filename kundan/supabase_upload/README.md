
### Run your app

1. Make sure **Streamlit** (or other dependencies) is installed in this environment:

```powershell
pip install -r requirements.txt
```

2. Then run your Streamlit app:

```powershell
streamlit run app.py
```

It should open a browser at:

```
http://localhost:8501
```
